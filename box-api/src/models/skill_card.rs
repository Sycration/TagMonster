/*
 * Box Platform API
 *
 * [Box Platform](https://box.dev) provides functionality to provide access to content stored within [Box](https://box.com). It provides endpoints for basic manipulation of files and folders, management of users within an enterprise, as well as more complex topics such as legal holds and retention policies.
 *
 * The version of the OpenAPI document: 2024.0
 * Contact: devrel@box.com
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// SkillCard : Box Skill card.
/// Box Skill card.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
#[serde(untagged)]
pub enum SkillCard {
    KeywordSkillCard(Box<models::KeywordSkillCard>),
    TimelineSkillCard(Box<models::TimelineSkillCard>),
    TranscriptSkillCard(Box<models::TranscriptSkillCard>),
    StatusSkillCard(Box<models::StatusSkillCard>),
}

impl Default for SkillCard {
    fn default() -> Self {
        Self::KeywordSkillCard(Default::default())
    }
}
/// The value will always be `skill_card`.
#[derive(Clone, Copy, Debug, Eq, PartialEq, Ord, PartialOrd, Hash, Serialize, Deserialize)]
pub enum Type {
    #[serde(rename = "skill_card")]
    SkillCard,
}

impl Default for Type {
    fn default() -> Type {
        Self::SkillCard
    }
}
/// The value will always be `status`.
#[derive(Clone, Copy, Debug, Eq, PartialEq, Ord, PartialOrd, Hash, Serialize, Deserialize)]
pub enum SkillCardType {
    #[serde(rename = "status")]
    Status,
}

impl Default for SkillCardType {
    fn default() -> SkillCardType {
        Self::Status
    }
}

